defmodule LauferDev.NvimConfig.HelperBotTest do
  use ExUnit.Case
  doctest LauferDev.NvimConfig.HelperBot

  test "greets the world" do
    assert LauferDev.NvimConfig.HelperBot.hello() == :world
  end
end
